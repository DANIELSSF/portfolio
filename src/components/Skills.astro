---
import { getLangFromUrl, t } from "@/i18n/translations";
const lang = getLangFromUrl(Astro.url);

interface Skill {
  name: string;
  slug: string | null;
}

/* ── Row 1: Languages + Backend ── */
const row1: Skill[] = [
  { name: "JavaScript", slug: "javascript" },
  { name: "TypeScript", slug: "typescript" },
  { name: "Go", slug: "go" },
  { name: "SQL", slug: null },
  { name: "NoSQL", slug: null },
  { name: "Node.js", slug: "nodedotjs" },
  { name: "NestJS", slug: "nestjs" },
  { name: "Express", slug: "express" },
  { name: "REST APIs", slug: null },
  { name: "Microservicios", slug: null },
  { name: "SOLID", slug: null },
  { name: "Clean Code", slug: null },
  { name: "Swagger", slug: "swagger" },
];

/* ── Row 2: Frontend + Databases ── */
const row2: Skill[] = [
  { name: "React", slug: "react" },
  { name: "Next.js", slug: "nextdotjs" },
  { name: "Astro", slug: "astro" },
  { name: "Tailwind CSS", slug: "tailwindcss" },
  { name: "MongoDB", slug: "mongodb" },
  { name: "PostgreSQL", slug: "postgresql" },
  { name: "MySQL", slug: "mysql" },
  { name: "OracleSQL", slug: null },
];

/* ── Row 3: Cloud & DevOps + Integrations ── */
const row3: Skill[] = [
  { name: "GCP", slug: "googlecloud" },
  { name: "AWS", slug: null },
  { name: "Docker", slug: "docker" },
  { name: "Git", slug: "git" },
  { name: "GitHub", slug: "github" },
  { name: "OpenAI API", slug: null },
  { name: "Claude Code", slug: "anthropic" },
  { name: "Meta API", slug: "meta" },
  { name: "Google Calendar", slug: "googlecalendar" },
  { name: "PayPal", slug: "paypal" },
  { name: "LLM Workflows", slug: null },
  { name: "MCP Integration", slug: null },
];

const iconUrl = (slug: string) => `https://cdn.simpleicons.org/${slug}/white`;
---

<section id="skills" class="py-24">
  <div class="mx-auto max-w-5xl px-6">
    <h2 class="mb-2 text-sm font-semibold uppercase tracking-widest text-accent">{t(lang, "skills.subtitle")}</h2>
    <h3 class="text-3xl font-bold tracking-tight text-title-section sm:text-4xl">{t(lang, "skills.title")}</h3>
  </div>

  {/* ── Marquee container (global CSS) ── */}
  <div class="marquee-container">

    {/* Row 1: scroll left (marquee-track) */}
    <div class="marquee-track">
      <div class="marquee-content">
        {row1.map((skill) => (
          <span class="skill-pill">
            {skill.slug ? (
              <img
                src={iconUrl(skill.slug)}
                alt=""
                width="16"
                height="16"
                loading="lazy"
                class="skill-icon"
                onerror="this.style.display='none';this.nextElementSibling.style.display='inline-flex'"
              />
              <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" class="skill-icon-fallback" style="display:none">
                <polyline points="16 18 22 12 16 6" />
                <polyline points="8 6 2 12 8 18" />
              </svg>
            ) : (
              <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" class="skill-icon-fallback">
                <polyline points="16 18 22 12 16 6" />
                <polyline points="8 6 2 12 8 18" />
              </svg>
            )}
            <span>{skill.name}</span>
          </span>
        ))}
      </div>
      <div class="marquee-content" aria-hidden="true">
        {row1.map((skill) => (
          <span class="skill-pill">
            {skill.slug ? (
              <img
                src={iconUrl(skill.slug)}
                alt=""
                width="16"
                height="16"
                loading="lazy"
                class="skill-icon"
                onerror="this.style.display='none';this.nextElementSibling.style.display='inline-flex'"
              />
              <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" class="skill-icon-fallback" style="display:none">
                <polyline points="16 18 22 12 16 6" />
                <polyline points="8 6 2 12 8 18" />
              </svg>
            ) : (
              <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" class="skill-icon-fallback">
                <polyline points="16 18 22 12 16 6" />
                <polyline points="8 6 2 12 8 18" />
              </svg>
            )}
            <span>{skill.name}</span>
          </span>
        ))}
      </div>
    </div>

    {/* Row 2: scroll right (marquee-track-reverse) */}
    <div class="marquee-track-reverse">
      <div class="marquee-content">
        {row2.map((skill) => (
          <span class="skill-pill">
            {skill.slug ? (
              <img
                src={iconUrl(skill.slug)}
                alt=""
                width="16"
                height="16"
                loading="lazy"
                class="skill-icon"
                onerror="this.style.display='none';this.nextElementSibling.style.display='inline-flex'"
              />
              <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" class="skill-icon-fallback" style="display:none">
                <polyline points="16 18 22 12 16 6" />
                <polyline points="8 6 2 12 8 18" />
              </svg>
            ) : (
              <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" class="skill-icon-fallback">
                <polyline points="16 18 22 12 16 6" />
                <polyline points="8 6 2 12 8 18" />
              </svg>
            )}
            <span>{skill.name}</span>
          </span>
        ))}
      </div>
      <div class="marquee-content" aria-hidden="true">
        {row2.map((skill) => (
          <span class="skill-pill">
            {skill.slug ? (
              <img
                src={iconUrl(skill.slug)}
                alt=""
                width="16"
                height="16"
                loading="lazy"
                class="skill-icon"
                onerror="this.style.display='none';this.nextElementSibling.style.display='inline-flex'"
              />
              <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" class="skill-icon-fallback" style="display:none">
                <polyline points="16 18 22 12 16 6" />
                <polyline points="8 6 2 12 8 18" />
              </svg>
            ) : (
              <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" class="skill-icon-fallback">
                <polyline points="16 18 22 12 16 6" />
                <polyline points="8 6 2 12 8 18" />
              </svg>
            )}
            <span>{skill.name}</span>
          </span>
        ))}
      </div>
    </div>

    {/* Row 3: scroll left (marquee-track) */}
    <div class="marquee-track">
      <div class="marquee-content">
        {row3.map((skill) => (
          <span class="skill-pill">
            {skill.slug ? (
              <img
                src={iconUrl(skill.slug)}
                alt=""
                width="16"
                height="16"
                loading="lazy"
                class="skill-icon"
                onerror="this.style.display='none';this.nextElementSibling.style.display='inline-flex'"
              />
              <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" class="skill-icon-fallback" style="display:none">
                <polyline points="16 18 22 12 16 6" />
                <polyline points="8 6 2 12 8 18" />
              </svg>
            ) : (
              <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" class="skill-icon-fallback">
                <polyline points="16 18 22 12 16 6" />
                <polyline points="8 6 2 12 8 18" />
              </svg>
            )}
            <span>{skill.name}</span>
          </span>
        ))}
      </div>
      <div class="marquee-content" aria-hidden="true">
        {row3.map((skill) => (
          <span class="skill-pill">
            {skill.slug ? (
              <img
                src={iconUrl(skill.slug)}
                alt=""
                width="16"
                height="16"
                loading="lazy"
                class="skill-icon"
                onerror="this.style.display='none';this.nextElementSibling.style.display='inline-flex'"
              />
              <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" class="skill-icon-fallback" style="display:none">
                <polyline points="16 18 22 12 16 6" />
                <polyline points="8 6 2 12 8 18" />
              </svg>
            ) : (
              <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" class="skill-icon-fallback">
                <polyline points="16 18 22 12 16 6" />
                <polyline points="8 6 2 12 8 18" />
              </svg>
            )}
            <span>{skill.name}</span>
          </span>
        ))}
      </div>
    </div>

  </div>
</section>
